import android.view.View
import androidx.recyclerview.widget.RecyclerView
import com.krishna.pro.databinding.SendMsgBinding

class SendMsgHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
    val binding: SendMsgBinding = SendMsgBinding.bind(itemView)
    // Add any other properties or methods related to the sent message view here
}
